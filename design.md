# StudyMeta 학생용 UI 설계 (v0.6)

> `index.html`의 Learner Model 데모(Evidence → Student State → Teaching Decision)는
> **설계자/연구자용** 화면이다. 이 문서는 같은 엔진(러너 모델)을 **학생에게 직접 노출되는 화면**으로
> 어떻게 번역할지 정의한다. 리서치 근거는 `md/ui-research.md` 참고.
>
> - **v0.2**: "전체 학습 현황(과목별)" 화면 추가, index.html 대비 반영 정확도 검토 섹션 추가.
> - **v0.3**: State/과목 집계 로직을 만들지 않기로 결정 — 스킬 카드는 합산 게이지 대신 개별
>   State 문구·배지를 그대로 노출하고, 과목은 서로 합치지 않고 탭으로 완전히 분리해서 보여준다.
> - **v0.4**: 타깃을 **대학생 전공 공부**로 구체화. "개인 과외쌤이 학습 플래너를 짜주는" 컨셉을
>   추가해 (1) 과목은 프리셋이 아니라 학생이 직접 등록, (2) 학습자료 업로드, (3) 시험 일정 입력
>   (과목당 여러 개), (4) 온보딩 진단 테스트는 제거하고 프론티어 모델의 자료 분석·문제풀이 과정에
>   위임, (5) 신규 화면 C "학습 플래너"(드래그 편집 가능)를 추가했다.
> - **v0.5**: 전체 서비스 프로토타입(`service-prototype.html`)을 만들면서 확정된 세부사항을
>   15번에 정리했다. 14번 "다음 단계"의 2·3·4·5번이 여기서 해소됐다.
> - **v0.6**: 학습이 일어나는 위치를 재정의했다. **StudyMeta 홈에서는 문제를 풀지 않는다.**
>   실제 학습(문제 풀이·힌트·설명)은 MCP로 연결된 프론티어 모델 앱(Claude, ChatGPT 등)에서
>   진행되고, 홈은 그 결과로 쌓인 **학습 현황(A)·최근 학습 기록(B)·플래너(C)만** 보여준다.
>   화면 B는 "문제 풀이 직후 즉시 피드백"에서 **"최근 학습 기록"**으로 성격이 바뀌었고,
>   "다음 문제로 이어 풀기" 같은 인앱 연속 학습 루프는 없앴다(16번 참고). 온보딩에 "학습 연결"
>   단계를 추가했고, 데모 데이터는 과목 1개(미적분학)로 줄였다.

---

## 1. 배경 및 목적

- 대상: **전공 공부를 하는 대학생**. "개인 과외쌤이 옆에서 학습 계획을 짜주고, 풀 때마다
  피드백을 주는" 경험을 목표로 한다.
- 러너 모델(Evidence 추출 → State 업데이트 → Teaching Decision 산출) 로직은 그대로 유지한다.
- 다만 `misconception_candidate`, `state_confidence` 같은 내부 변수명·Evidence/State 딕셔너리
  원문은 **개발자 전용 개념**이며, 학생에게는 그대로 노출하지 않는다.
- 목적: 학생이 (1) 과목별 자료를 올려두고 시험 일정을 등록하면, (2) 시험일까지의 학습 플래너를
  자동으로 받고, (3) MCP로 연결한 프론티어 모델 앱(Claude, ChatGPT 등)에서 공부한 결과를
  기록으로 확인하며, (4) 여러 과목에 걸친 학습 현황을 한눈에 관리할 수 있게 한다.
- **StudyMeta 홈에서는 학습(문제 풀이)이 일어나지 않는다.** 홈은 러너 모델이 만든 상태를
  보여주고 계획을 세워주는 "관제탑"이고, 실제 풀이·힌트·설명 같은 학습 행위는 학생이 이미
  쓰는 프론티어 모델 앱 쪽에서 이루어진다(16번 참고).

## 2. 이번 범위(Scope) 결정 사항

| 항목 | 결정 |
|---|---|
| 주 사용자 | 전공 공부 중인 대학생 (교사/연구자용 화면은 이번 범위 아님) |
| 스케일 | **화면 3개 + 온보딩**: (A) 과목별 전체 학습 현황, (B) 최근 학습 기록(MCP로 들어온 결과 열람), (C) 학습 플래너(상시 화면). 여러 "학생"을 비교하는 교사용 대시보드는 이번 범위 제외 |
| 과목 등록 방식 | 프리셋에서 선택(❌) → **학생이 과목명을 직접 입력해 등록**. 언제든 추가 가능 |
| 학습자료 업로드 | 과목 등록 시 **스킵 가능**, 이후 언제든 추가 가능 |
| 시험 일정 | 과목당 **여러 개 등록 가능**(중간/기말/퀴즈 등 각각 이름+날짜) |
| 온보딩 진단 테스트 | **제거**. 별도 진단 퀴즈 대신, 업로드된 자료 분석과 실제 문제풀이·대화 과정에서 State가 쌓이도록 프론티어 모델에 위임 |
| 학습이 일어나는 위치 | **홈이 아니라 MCP로 연결된 프론티어 모델 앱**. 홈은 학습 현황·최근 기록·플래너만 보여주는 관제탑 (16번 참고) |
| 학습 플래너 편집 | **드래그로 일정 재배치 가능** (v1부터 포함) |
| 시간축(망각곡선) | Retrievability 등은 여전히 **그래프 없이** 배지/짧은 문구로 표현 (플래너의 "날짜별 계획"과는 별개 결정) |
| Evidence-State 관계 노출 | 기본은 자연어 설명 1~2줄, "자세히 보기"로 색상 강조된 카드까지 옵트인 확장. 연결선 다이어그램은 만들지 않음 |
| 집계(Aggregation) | State/과목을 합산한 종합 점수는 만들지 않음 (10번 참고) |

## 3. 전체 UX 플로우 개요

```
[가입/로그인] → [온보딩: 과목 등록 → 학습 연결] → [화면 A: 전체 학습 현황] ◀───────┐
                                                        │                     │
                    ┌───────────────────────┼───────────────────────┐         │
                    ▼                       ▼                       ▼         │
            [화면 A′: 스킬 상세]      [화면 C: 학습 플래너]      (과목 탭 전환)     │
                    │                       │                                 │
          "최근 학습 기록 보기"       시험 선택 → 일별 계획                        │
                    │                  (드래그로 재배치 가능)                    │
                    ▼                       │                                 │
            [화면 B: 최근 학습 기록] ─────"현황으로 돌아가기"──────────────────────┘
                    │
              "자세히 보기" ──▶ [화면 B′: Evidence/State 상세]

  ※ 실제 문제 풀이는 이 플로우 밖, MCP로 연결된 프론티어 모델 앱에서 일어난다.
    그 결과가 학습 이벤트로 들어오면 화면 A·B·C가 갱신된다. 16번 참고.
```

## 4. 온보딩 플로우

```
[가입/로그인]  (이메일 또는 소셜 로그인 — 인증 수단 자체는 이번 설계 범위 밖)
   │
   ▼
[1] 기본 정보: 학교/학년/전공 (선택, 최소한만)
   │
   ▼
[2] 과목(강의) 등록 — 반복 가능, 최소 1과목 필요
   │
   ├─ 과목명 입력 (자유 텍스트, 프리셋 아님)
   ├─ 학습자료 업로드 (PDF/노트 등, 스킵 가능)
   └─ 시험 일정 입력 (이름+날짜, 0개 이상, 중간/기말/퀴즈 등 여러 개 가능)
   │
   └─ "+ 과목 추가"로 반복 ──▶ 다 등록했으면 "완료"
   │
   ▼
[3] 자료 분석 중 (업로드한 자료가 있는 과목만)
   │   프론티어 모델이 자료에서 개념/스킬을 추출. 별도 진단 퀴즈는 없음 —
   │   이후 실제 문제풀이·대화 과정에서 State가 자연스럽게 채워짐
   │   자료 없는 과목은 이 단계 스킵, 스킬 목록 빈 상태로 시작
   ▼
[4] 학습 연결 (MCP) — Claude/ChatGPT 등 프론티어 모델 앱과 연결
   │   여기서 문제를 풀지 않는다. 연결만 해두면 이후 해당 앱에서 공부한 결과가
   │   학습 이벤트로 들어와 화면 A·B·C에 반영된다. 이 단계도 스킵 가능(나중에 연결)
   ▼
[5] 학습 플래너 초안 생성 (시험 일정을 입력한 과목만 대상, 화면 C 데이터 시딩)
   │
   ▼
[화면 A] 최초 진입 — 등록한 과목들이 탭으로 표시
```

### 온보딩 엣지 케이스

| 상황 | 처리 |
|---|---|
| 과목 0개로 완료 시도 | 진행 불가, "최소 1개 과목을 등록해주세요" |
| 자료 업로드 스킵 | 해당 과목은 스킬 목록이 빈 상태(4-4의 "아직 시작 전")로 시작, 이후 언제든 자료 추가 가능 |
| 시험 일정 미입력 | 해당 과목은 화면 C(학습 플래너)에 노출되지 않음(플래너는 시험일 기준으로 계획을 만들기 때문). 이후 시험 일정 추가 시 자동 반영 |
| 과목 등록 도중 이탈 | 그때까지 등록한 과목만 저장, 나머지는 화면 A의 "+ 과목 추가"에서 이어서 등록 |
| 재로그인(기존 사용자) | 온보딩 스킵, 화면 A로 바로 진입 |
| 학습 연결을 건너뜀 | 과목·시험 일정은 그대로 저장되고 화면 A/C는 정상 동작. 다만 연결 전에는 화면 B에 새 기록이 쌓이지 않음. 연결은 화면 A 어디서든 나중에 가능(설정, 17번 참고) |

> 과목 등록·자료 업로드·시험 일정 입력은 온보딩 전용이 아니라, **화면 A의 "+ 과목 추가" 탭과
> 각 과목 헤더의 "자료/일정 추가" 버튼**을 통해 이후에도 언제든 가능해야 한다.

## 5. 화면 A: 전체 학습 현황 (과목별 대시보드)

### 5-1. 목적

학생이 등록한 여러 과목에 걸쳐 "지금 뭘 잘하고 있고, 뭘 더 챙겨야 하는지"를 스스로 관리하는
진입 화면.

### 5-2. 정보 구조: 과목(Domain) → 스킬(Skill)

러너 모델의 State 카테고리가 이미 `STUDENT × SKILL`(스킬 단위 7개 State)과
`STUDENT × DOMAIN`(과목 단위 State: calibration)으로 나뉘어 있다. 이 계층을 그대로
화면 구조에 사용하되, "과목"은 학생이 직접 등록한 강의명이다(4번 온보딩 참고).

### 5-3. 화면 구조 (와이어프레임 텍스트)

```
┌─────────────────────────────────────────┐
│ 과목 탭:  [미적분학] [화학] [경제학원론] [+ 과목 추가] │
├─────────────────────────────────────────┤
│ 과목 요약 배너 (STUDENT × DOMAIN state)     │
│   예: "예상과 실제가 꽤 잘 맞고 있어요"        │
│   [플래너 보기 →] (화면 C로, 이 과목 필터 적용)   │
├─────────────────────────────────────────┤
│ 스킬 카드 그리드 (STUDENT × SKILL, 스킬별 독립 표시) │
│  [카드] Chain Rule     능숙해지고 있어요         │
│  [카드] 극한의 정의      복습 추천 배지            │
│  [카드] 테일러 급수      확인 필요 배지            │
├─────────────────────────────────────────┤
│ 정렬/필터: "약한 스킬 먼저 보기" 토글          │
└─────────────────────────────────────────┘
```

- 스킬 카드는 Khan Academy의 스킬 타일 그리드, ALEKS의 파이 차트 드릴다운 패턴을 참고했지만,
  값을 합산해 하나의 점수로 만들지는 않는다(12번 참고).
- 스킬의 출처는 그 과목에 업로드된 자료에서 추출된 개념이다. 자료가 없으면 스킬 목록이 비어
  있고, "자료를 올려서 학습을 시작해보세요" 안내만 노출.
- "약한 스킬 먼저 보기" 정렬이 학생이 스스로 학습을 관리하게 돕는 핵심 액션.

### 5-4. 스킬 카드 요약 로직 (7개 SKILL State → 카드 1장)

**집계(여러 State/여러 세션을 하나의 점수로 합산)는 하지 않는다.** 모델이 갖고 있는 State
값을 그대로 보여준다. 스킬은 과목 탭 안에서 각각 독립적으로 표시되며, 스킬끼리 또는
과목끼리 합쳐서 하나의 종합 점수를 만들지 않는다.

카드에는 7개 State를 모두 나열하지 않고, 그중 학생이 체감할 수 있는 항목만 우선 노출한다.

| 표시 요소 | 반영 State | 규칙(제안) |
|---|---|---|
| 상태 표시(예: "능숙해지고 있어요") | `conceptual_mastery`, `procedural_mastery` | 두 값을 합산하지 않고 각각 짧은 문구/아이콘으로 그대로 표시 |
| "복습 추천" 배지 | `retrievability` 낮음 | 그래프·점수화 없이 배지 한 개로만 표시(시간축 결정 유지) |
| "확인 필요" 배지 | `misconception` 있음 | 반복 오류 후보가 있을 때만 노출 |
| "도움이 필요해요" 배지 | `help_need` 높음 | 최근 힌트 의존도가 높을 때만 노출 |
| 자세히 보기 전용 | `transferability`, `state_confidence` | 카드에는 안 보이고, 스킬 카드 클릭 시 펼치는 상세에서만 노출 |

- 카드 자체는 상태 문구 1줄 + 배지 최대 2개까지만.
- 배지가 여러 개 해당될 경우 우선순위: **확인 필요(오개념) > 도움이 필요해요 > 복습 추천**.
- 아직 한 번도 안 푼 스킬: 상태 문구 대신 "아직 시작 전" + 연결된 앱에서 공부해보라는 안내문만 노출
  (홈에는 "풀어보기" 버튼이 없다 — 16번 참고).
- 모든 스킬이 양호(배지 없음): 배지 대신 격려 문구 카드로 대체(예: "지금처럼만 하면 돼요").

### 5-5. 과목 레벨 위젯 (DOMAIN / DOMAIN×INTERVENTION State)

- `calibration`(STUDENT × DOMAIN): 과목 요약 배너에 "예상과 실제가 얼마나 맞았는지" 한 문장으로.
- `intervention_response`(STUDENT × DOMAIN × INTERVENTION): 학생에게 직접 노출하지 않음
  (내부 튜닝용 지표 — 화면 B의 "자세히 보기"에서도 제외 대상으로 유지).

### 5-6. 스킬 카드 클릭 시 동작

카드를 누르면 같은 화면에서 아래로 펼쳐지는 "스킬 상세" 패널(화면 A′)을 연다(새 화면 전환
없음, 화면 B의 "자세히 보기"와 동일한 옵트인 확장 패턴 재사용):

- 최근 풀이 이력 요약(짧게), 7개 SKILL State 전체를 8장 콘텐츠 매핑 라벨로 표시.
- "최근 학습 기록 보기" CTA 버튼 → 화면 B(최근 학습 기록)로 이동. 아직 기록이 없는 스킬은
  버튼 대신 "연결된 앱에서 공부해보세요" 안내문을 보여준다(16번 참고).

## 6. 화면 B: 최근 학습 기록

MCP로 연결된 프론티어 모델 앱에서 학습 이벤트가 들어오면, 그 결과를 확인하는 화면이다.
이 화면 자체에서 문제를 풀지는 않는다 — "방금 이렇게 달라졌어요"가 아니라 "이 스킬은
최근 이렇게 기록됐어요"에 가깝다. 화면 A의 스킬 카드에서 "최근 학습 기록 보기"를 누르면
진입한다.

한 화면에 위에서 아래로 4개 블록. 기본 노출은 1~3번, 4번은 접힌 상태(옵트인).

```
┌─────────────────────────────────────────┐
│ ① 결과 배너                                │
│   정답/오답 아이콘 + 한 줄 반응 문구           │
├─────────────────────────────────────────┤
│ ② 이번에 달라진 점 (최대 2~3장 카드)          │
│   [카드] 아이콘 + 친화적 State 문구 + 방향    │
├─────────────────────────────────────────┤
│ ③ 다음 추천 행동 (Teaching Decision)        │
│   강조 카드 1개, 명령이 아닌 제안 톤           │
├─────────────────────────────────────────┤
│ ▾ 자세히 보기 (기본 접힘, 옵트인)             │
│   - 감지된 Evidence 칩(색상 강조)            │
│   - 관련 State 카드 전체(색상 강조)           │
└─────────────────────────────────────────┘
```

- **① 결과 배너**: 정답/오답을 즉각적·감정적으로 전달 (Duolingo류 초록/빨강 배너 패턴).
- **② 상태 변화 카드**: `index.html`의 `stateUpdates` 중 변화가 있는 항목만, 최대 2~3장만 우선순위로
  노출한다(전부 나열하면 정보 과다). 우선순위 기준: `state_confidence`처럼 메타적인 항목보다
  `conceptual_mastery`, `procedural_mastery`, `misconception`처럼 학생이 체감할 수 있는 항목을 우선.
- **③ 다음 추천 행동**: `decisionTitle`/`decision`을 학생에게 말하듯 1인칭 유도형으로 재작성한다
  (교사 지시문이 아니라 학생에게 건네는 제안).
- **④ 자세히 보기**: 펼치면 지금 데모의 Evidence Dictionary/Student State 패턴(하이라이트 카드)을
  그대로 재사용하되, **원본 코드(`code` 태그의 snake_case id)는 학생 화면에 노출하지 않고** 사람이
  읽는 라벨/설명만 보여준다.
- 새 학습 이벤트가 들어오면 화면 A의 해당 스킬 카드, 화면 C의 해당 날짜 계획이 갱신된
  상태로 반영된다.
- **출처 표시**: 이 기록이 어느 앱(Claude/ChatGPT 등)에서, 언제 들어왔는지를 결과 배너
  위에 짧게 보여준다(예: "🔗 Claude에서 온 기록 · 어제"). 학습이 홈 밖에서 일어난다는
  사실을 학생이 늘 알 수 있게 하기 위함이다.

### 화면 B → 다음 행동 분기

| 학생 액션 | 결과 |
|---|---|
| "현황으로 돌아가기" | 화면 A로 복귀, 방금 확인한 스킬의 카드가 그대로 보임 |
| 이어서 더 공부하고 싶을 때 | 화면 B는 "다음 문제" 버튼을 제공하지 않는다. 대신 "이어서 공부하려면 (연결된 앱)로 돌아가세요"라는 안내문으로 유도한다 — 연속 학습 루프는 프론티어 모델 앱 쪽의 몫이다 |
| 아무 것도 안 누르고 이탈 | 화면 A로 복귀와 동일하게 처리(자동 저장) |
| 서버 응답 지연 | ①~③ 자리에 스켈레톤 로딩, ④는 응답 도착 전까지 비활성 |

## 7. 화면 C: 학습 플래너 (신규, 상시 화면)

"개인 과외쌤이 짜주는 계획표" 역할. 화면 A/B와 별개의 탭으로 상시 접근 가능하며, 화면 A의
과목 헤더 "플래너 보기"에서도 진입 가능(해당 과목 필터 적용).

### 7-1. 목적

여러 과목의 시험 일정을 한눈에 조율하고, 시험일까지 무엇을 언제 공부할지 추천받는다.
과목 단위가 아니라 **전체 과목을 통합한 뷰**를 기본으로 한다 — 실제 과외쌤이 한 학생의
여러 과목 시험을 함께 조율하는 것과 같은 관점.

### 7-2. 화면 구조

```
┌─────────────────────────────────────────┐
│ C-1. 전체 시험 일정 개요 (D-day 순 정렬)      │
│   D-12  화학 - 중간고사                     │
│   D-20  경제학원론 - 기말고사                  │
│   D-45  화학 - 기말고사                      │
├─────────────────────────────────────────┤
│ (시험 하나 선택 시)                          │
│ C-2. 해당 시험까지의 일별 학습 계획             │
│   오늘   [카드] 극한의 정의 복습하기 (복습 추천)   │
│          [카드] 테일러 급수 확인하기 (확인 필요)   │
│   내일   [카드] ...                         │
│   ...                                     │
│   (카드를 다른 날짜로 드래그해 일정 재배치 가능)     │
└─────────────────────────────────────────┘
```

### 7-3. 배치 규칙

- 일별 카드는 5-4의 배지 우선순위(**확인 필요 > 도움이 필요해요 > 복습 추천**)를 그대로
  재사용해, 임박한 시험 앞에 우선 배치한다.
- **집계 로직은 만들지 않는다**: 여러 스킬을 합쳐 "오늘의 학습 점수"를 만들지 않고, 스킬별
  카드를 개별적으로 나열한다(12번 결정과 일관).
- 여러 과목의 시험이 겹칠 경우, 임박한 시험(D-day가 작은 쪽)의 스킬을 우선 배치.

### 7-4. 드래그 편집

- 학생이 카드를 다른 날짜 칸으로 드래그하면 계획을 직접 재배치할 수 있다(v1부터 포함).
- 재배치는 로컬 일정 조정일 뿐 State 자체를 바꾸지 않는다 — 계획을 옮겨도 실제 숙련도
  판단(러너 모델)에는 영향 없음.
- 시험일 이후로 드래그하는 것은 막는다(경고 후 취소).
- 카드를 완료 처리(체크)하면 배지가 사라지고 계획에서 제외된다.

### 7-5. 리서치 근거

- Khan Academy: 스킬 = 타일 그리드 → 카드 구조에 반영(합산 점수화는 하지 않음).
- ALEKS: 파이 차트 요약 후 드릴다운 → 시험 선택 시 상세 계획을 펼치는 패턴에 반영.
- Anki/FSRS의 "복습 시점" 개념을 그래프 없이 배지+날짜 배치로 단순화해서 반영.

## 8. 콘텐츠 매핑: 내부 개념 → 학생 문구 (화면 A/B/C 공통)

### 8-1. Evidence → 학생 노출 라벨 (자세히 보기에서만 노출)

| 카테고리 | 내부 id | 학생 라벨 |
|---|---|---|
| PERFORMANCE | correct | 정답 |
| | incorrect | 오답 |
| | partial_success | 부분적으로 맞음 |
| ASSISTANCE | independent_success | 혼자 힘으로 해결 |
| | hint_requested | 힌트 요청 |
| | success_after_hint | 힌트 받고 해결 |
| | success_after_explanation | 설명 듣고 해결 |
| ERROR | first_error | 첫 실수 |
| | repeated_error | 반복되는 실수 |
| | misconception_candidate | 헷갈리는 개념 후보 |
| | self_correction | 스스로 수정함 |
| MEMORY/RETRIEVAL | immediate_retrieval_success | 바로 기억해 냄 |
| | delayed_retrieval_success | 시간이 지나도 기억함 |
| | delayed_retrieval_failure | 시간이 지나 잊어버림 |
| | relearning | 다시 배우는 중 |
| TRANSFER | novel_application_success | 새로운 문제에 응용 성공 |
| | novel_application_failure | 새로운 문제에 응용 실패 |
| METACOGNITION | confidence_report | 확신 정도 응답 |
| | perceived_understanding | "이해했다"는 응답 |
| | perceived_difficulty | 체감 난이도 응답 |
| | error_awareness | 실수를 스스로 알아챔 |
| PROCESS | response_time | 풀이 소요 시간 |
| | stuck_duration | 막힌 시간 |
| | attempt_count | 시도 횟수 |

### 8-2. State → 학생 노출 문구

| 내부 id | 카테고리 | 학생 문구(변화 있을 때) | 화면 A 카드 노출 | 화면 B 카드 노출 |
|---|---|---|---|---|
| conceptual_mastery | SKILL | "개념 이해가 [좋아지고/약해지고] 있어요" | 상태 문구로 표시(합산 없음) | 카드 |
| procedural_mastery | SKILL | "풀이 과정이 [능숙해지고/서툴러지고] 있어요" | 상태 문구로 표시(합산 없음) | 카드 |
| retrievability | SKILL | "이 내용을 [잘 기억하고/까먹기 시작했고] 있어요" | "복습 추천" 배지 | 카드 |
| transferability | SKILL | "다른 문제에도 잘 [적용하고/적용하기 어려워하고] 있어요" | 자세히 보기 전용 | 자세히 보기 전용 |
| help_need | SKILL | "필요한 도움이 [줄고/늘고] 있어요" | "도움이 필요해요" 배지 | 카드 |
| misconception | SKILL | "헷갈리는 부분이 [있는 것 같아요/줄고 있어요]" | "확인 필요" 배지 | 카드 |
| state_confidence | SKILL | *(학생 화면 기본 미노출 — 내부 신뢰도 지표)* | 자세히 보기 전용 | 자세히 보기 전용 |
| calibration | DOMAIN | "예상한 것과 실제 결과를 비교해볼게요" | 과목 요약 배너 | 자세히 보기 전용 |
| intervention_response | DOMAIN×INTERVENTION | *(내부 튜닝용, 학생 비노출)* | 미노출 | 미노출 |

### 8-3. Teaching Decision → 학생 제안 문구 톤 (화면 B ③)

- 지시형(교사용) 대신 **제안형·1인칭 유도형**으로 재작성.
  - 예) "개입을 줄이고 독립 수행을 확인" → "다음엔 힌트 없이 한번 도전해볼까요?"
  - 예) "간격 회상 연습을 우선" → "잠깐 다시 떠올려볼 시간이에요."
- Khanmigo 스타일 원칙 적용: **정답/결론을 대신 말해주지 않고, 다음 행동을 부드럽게 제안**한다.

## 9. 인터랙션 패턴

- **기본 노출 최소화 + 옵트인 확장(progressive disclosure)**: 화면 B는 로드 시 ①②③만 보이고,
  ④는 "자세히 보기" 토글로 펼친다. 화면 A의 스킬 카드도 동일하게 "클릭 시 아래로 펼치기" 방식.
- 옵트인 확장 시에만 `index.html`의 색상 강조(`is-active`) 패턴을 재사용 — 감지된 Evidence 칩과
  관련 State 카드에 파란색 강조. 다만 코드/`code` 태그는 제거하고 라벨+설명만 표시.
- 연결선 다이어그램(Evidence↔State를 선으로 잇는 관계도)은 만들지 않는다.
- 화면 A ↔ 화면 B 전환: 스킬 카드의 "최근 학습 기록 보기" CTA → 화면 B, "현황으로 돌아가기"
  버튼(또는 뒤로가기) 시 화면 A로 복귀해 갱신된 상태를 보여준다.
- 화면 A ↔ 화면 C 전환: 과목 헤더 "플래너 보기" → 화면 C(해당 과목 필터).
- 화면 C 내 드래그: 카드를 날짜 칸 사이에서 드래그해 재배치(7-4 참고).

## 10. 비주얼 가이드

- `index.html`의 디자인 토큰(색상 변수, radius, shadow)을 그대로 재사용해 톤을 통일한다.
  - `--blue`(강조/제안), `--green`(정답/긍정 변화), `--amber`(주의/헷갈림), `--red`(오답)를
    결과 배너·State 카드 방향(↑/↓)·배지 색상에 공통 활용.
- 카드형 레이아웃(`.section-card`, `.state-item` 스타일)을 계승하되, 학생 화면에서는
  타이포를 더 크게(가독성), 카드/배지 수를 최소화한다.

## 11. index.html 대비 반영 정확도 검토

| 항목 | 검토 결과 |
|---|---|
| Evidence Dictionary (7개 카테고리, 24개 항목) | 8-1 표에 전부 1:1 반영 확인. 누락 없음 |
| Student State (3개 그룹, 9개 항목) | 8-2 표에 전부 1:1 반영 확인. 누락 없음 |
| 시나리오 9개 | 화면 B ③의 톤 재작성 예시가 이 9개 시나리오의 `decision` 텍스트를 소스로 함 |
| Evidence → State 매핑 로직 | index.html의 `scenario.stateUpdates` 로직을 그대로 계승(화면 B ②) |
| **과목(Domain)/여러 스킬 데이터** | ⚠️ index.html에는 없음. 화면 A/C는 `STUDENT × DOMAIN` 계층을 실제 화면으로 구현하는 것이나, 여러 과목·여러 스킬 데이터 구조는 새로 필요 |
| **학습자료 업로드 / 시험 일정 / 학습 플래너** | ⚠️ index.html에 전혀 없는 완전히 새로운 기능. 러너 모델의 Evidence/State 구조는 그대로 재사용하지만, 자료 업로드→스킬 추출, 시험 일정 데이터, 플래너의 날짜별 배치 로직은 이번 v0.4에서 처음 정의됨(7번 참고) |
| 온보딩 진단 테스트 | index.html에는 애초에 온보딩 개념이 없음. v0.3 초안에서 제안했던 진단 퀴즈는 v0.4에서 제거 |

## 12. 집계(Aggregation) 관련 결정

- **여러 State 값을 하나의 점수로 합치는 로직은 만들지 않는다.** 스킬 카드·과목 요약·학습
  플래너 모두 모델이 갖고 있는 State 값을 있는 그대로(문구/배지) 보여준다.
- **과목끼리도 합쳐서 하나의 전체 진행도로 만들지 않는다.** 과목 탭으로 완전히 분리해서
  보여주고, 학습 플래너(화면 C)만 예외적으로 여러 과목의 **시험 일정**을 D-day 기준으로
  나열한다 — 이건 State를 합산하는 게 아니라 일정을 나열하는 것이므로 12번 원칙과 배치되지
  않는다.

## 13. 이번 범위에서 제외한 것 (Out of scope)

- Retrievability 등의 시계열/망각곡선 그래프 (화면 A/C에서도 배지·날짜 배치로만 대체)
- Evidence-State 관계를 잇는 hover 연결선 다이어그램
- 교사/연구자용 다중 "학생" 비교 대시보드 (원래 `index.html`이 담당하는 영역과는 별개로 유지)
- `intervention_response` 등 내부 튜닝 전용 지표의 학생 노출
- 여러 State/여러 과목을 합산하는 집계 로직 (12번 참고 — 의도적으로 만들지 않음)
- 로그인/인증 수단 자체의 상세 설계(이메일/소셜 등)

## 14. 다음 단계

1. 화면 A/C에 필요한 데이터 구조(과목 → 자료 → 추출된 스킬 목록, 과목 → 시험 목록 → 날짜)
   정의 — 11번 검토에서 확인된 갭을 메우는 작업.
2. 자료 업로드 → 스킬/개념 추출 로직의 처리 상태 UI(진행률, 실패 시 재시도) 상세 설계.
3. 화면 C의 드래그 재배치 인터랙션 상세 스펙(드래그 중 시각 피드백, 모바일 터치 대응) 설계.
4. 콘텐츠 매핑 표를 바탕으로 화면 A/B/C의 실제 와이어프레임(HTML) 초안 제작.
5. "자세히 보기" 확장 인터랙션을 `index.html`의 기존 JS 패턴(scenario 선택 로직) 재사용해 프로토타이핑.

## 15. 프로토타입 구현에서 확정된 세부사항 (v0.5)

`service-prototype.html`을 만들면서 정한 것들이다. 1~14번 결정은 그대로 두고, 그 위에서
"그럼 실제로 어떻게 동작하나"만 채웠다.

### 15-1. 가입 / 재방문

- 가입·로그인 화면은 이메일 + 소셜 버튼의 **목업**이다(인증 수단 자체는 13번대로 범위 밖).
- 온보딩 결과는 브라우저 `localStorage`에만 저장한다. 재방문 시 등록한 과목이 있으면
  온보딩을 건너뛰고 화면 A로 바로 들어간다(4번 엣지 케이스 "재로그인" 구현).
- 과목 등록 중 이탈했다가 돌아오면 그때까지 등록한 과목이 남은 채 온보딩 2단계로 복귀한다.

### 15-2. 자료 → 스킬 추출 (14번 2항)

- 스킬은 **아직 분석하지 않은 자료**에서만 새로 뽑고, 이미 있는 스킬은 건드리지 않는다.
  자료를 추가할 때마다 스킬 목록을 갈아끼우면 그동안 쌓인 State가 사라지기 때문이다.
- 처리 상태 UI: 과목별 진행 막대 + "스킬 N개를 새로 찾았어요" 문구. 분석을 건너뛸 수 있고,
  건너뛴 과목은 스킬 목록이 빈 상태로 시작한다.
- 새로 만들어진 스킬은 **"아직 시작 전"** 상태로, 배지 대신 연결된 앱에서 공부해보라는
  안내문만 노출한다(5-4의 마지막 규칙 — v0.6에서 "첫 문제 풀어보기" 버튼은 없앴다. 16번 참고).

### 15-3. MCP 학습 이벤트 → 화면 B 연결 (v0.6, 16번과 함께 읽기)

> 이 항목은 v0.5 당시 "홈 안에서 문제를 푼다"는 전제로 쓰였던 내용을 v0.6 기준으로
> 다시 정리한 것이다. 실제 문제 풀이 UI(연습 화면)는 v0.6에서 없앴다.

- 화면 B의 시나리오 선택기는 실제 문제 풀이 화면이 아니라, **MCP로 이런 학습 이벤트가
  들어왔다면 홈이 어떻게 반영하는지 보여주는 시연 도구**다. 선택 가능한 상황은
  **기존 9개 시나리오**로 한정한다. 새 시나리오를 만들지 않는다.
- 시나리오를 적용하면 그 결과가 화면 A·C에 나타난다. 이때 `stateUpdates`에 있는
  SKILL State만 방향을 갱신하고, 언급되지 않은 State는 "변화 없음"으로 그대로 둔다.
  값을 합쳐 점수를 만들지 않는다(12번과 일관).
- `calibration`은 과목 요약 배너 문구만 바꾼다. `intervention_response`는 어느 화면에도
  노출하지 않는다.
- 반영된 기록에는 **출처(어느 앱)와 시점**을 함께 저장해 화면 B 상단에 보여준다(6번 참고).

### 15-4. 플래너 배치·드래그 (14번 3항)

- 계획 카드는 **(시험 × 스킬 × 배지)** 단위로 만든다. 배지가 없는 스킬도 카드 한 장을 갖고,
  여러 스킬을 묶어 "오늘의 점수"를 만들지 않는다.
- 배치는 배지 우선순위(확인 필요 > 도움이 필요해요 > 복습 추천) 순으로 오늘부터 채운다.
  칸은 오늘부터 4일치이며, **시험일 이후 칸은 잠긴 상태**로 표시하고 드롭을 막는다.
- 드래그 중에는 대상 칸을 파란색으로 강조하고, 막힌 이동은 경고 문구로 알린다.
- 드래그가 어려운 터치 환경을 위해 카드마다 **◀ ▶ 하루 이동 버튼**을 함께 둔다.
- 재배치·완료 체크는 로컬 일정 조정일 뿐이라 State를 바꾸지 않는다(7-4 유지).

## 16. 학습이 일어나는 위치 (MCP 연결 아키텍처, v0.6)

### 16-1. 왜 바꿨나

StudyMeta 홈에서 직접 문제를 풀게 하는 대신, **학생이 이미 쓰는 프론티어 모델 앱
(Claude, ChatGPT 등)에서 공부하고, 그 결과만 StudyMeta로 들어오는 구조**로 바꿨다.
홈을 "학습지"가 아니라 "관제탑"으로 두는 결정이다. 학습 자체의 품질(설명, 힌트, 대화)은
프론티어 모델에 맡기고, StudyMeta는 그 결과를 모아 상태를 보여주고 계획을 세워주는
역할에 집중한다.

### 16-2. 데이터 흐름

```
[학생이 Claude/ChatGPT에서 공부]
        │  MCP get_my_learner_context: 세션 시작 시 지금까지의 상태를 읽음
        │  (프론티어 모델이 무엇을 이미 알고, 뭘 헷갈려 하는지 참고해서 대화)
        ▼
[문제 풀이 · 힌트 · 설명 · 오류 정정 — 전부 그 앱 안에서]
        │  MCP record_learning_event: 관찰을 원본 이벤트 + 구조화된 Evidence로 기록
        ▼
[러너 모델: Evidence → State 업데이트]  (index.html과 같은 로직)
        ▼
[StudyMeta 홈: 화면 A(현황) · B(최근 기록) · C(플래너)에 반영]
```

- 홈은 이 파이프라인의 **입력을 만들지 않는다.** 화면 A/B/C는 모두 읽기 전용 + 일정 재배치
  (플래너 드래그) 뿐이며, 문제를 내거나 채점하지 않는다.
- `record_learning_event`의 `source` 값(`chatgpt` / `claude` / `camera` / `quiz` / `manual`)이
  화면 B의 "어디서 온 기록인지" 표시에 대응한다.

### 16-3. 화면별 영향

| 화면 | v0.5까지 | v0.6부터 |
|---|---|---|
| A | 스킬 카드 → "연습하기" → 문제 풀이 진입 | 스킬 카드 → "최근 학습 기록 보기" → 화면 B (읽기 전용) |
| B | "문제 풀이 직후 즉시 피드백", 인앱에서 이어 풀기 가능 | "최근 학습 기록", 출처·시점 표시. 이어 풀기는 연결된 앱으로 안내 |
| C | 변화 없음 — 계획은 여전히 홈에서 세우고 드래그로 재배치 |

### 16-4. 온보딩 "학습 연결" 단계

- 온보딩 3(자료 분석)과 5(플래너 초안) 사이에 **[4] 학습 연결**을 추가했다(4번 참고).
- 여기서 계정을 만들지 않는다 — Claude/ChatGPT 쪽 커넥터 설정으로 연결하는 것을 안내하는
  화면이다. 스킵 가능하며, 스킵해도 과목·시험 등록은 그대로 저장된다.
- 연결 전에는 화면 B에 새 기록이 쌓이지 않지만, 화면 A/C는 정상 동작한다(4번 엣지 케이스).

## 17. 이번 범위에서 아직 정의하지 않은 것 (16번 관련)

- 여러 프론티어 모델 앱을 동시에 연결했을 때의 화면 표기(지금은 데모상 1개만 가정).
- 연결 해제/재연결 설정 화면의 위치와 UI.
- 카메라·퀴즈 등 다른 `source` 값이 실제로 어떤 입력 경험에서 오는지의 상세 설계.
- MCP 응답 지연·실패 시 화면 A/B의 로딩·에러 상태.
