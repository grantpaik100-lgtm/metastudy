# StudyMeta 학생용 UI 설계 (v0.3)

> `index.html`의 Learner Model 데모(Evidence → Student State → Teaching Decision)는
> **설계자/연구자용** 화면이다. 이 문서는 같은 엔진(러너 모델)을 **학생에게 직접 노출되는 화면**으로
> 어떻게 번역할지 정의한다. 리서치 근거는 `md/ui-research.md` 참고.
>
> **v0.2 변경**: "전체 학습 현황(과목별)" 화면 추가, index.html 대비 반영 정확도 검토 섹션 추가.
> **v0.3 변경**: State/과목 집계 로직을 만들지 않기로 결정 — 스킬 카드는 합산 게이지 대신
> 개별 State 문구·배지를 그대로 노출하고, 과목은 서로 합치지 않고 탭으로 완전히 분리해서 보여준다.

---

## 1. 배경 및 목적

- 러너 모델(Evidence 추출 → State 업데이트 → Teaching Decision 산출) 로직은 그대로 유지한다.
- 다만 `misconception_candidate`, `state_confidence` 같은 내부 변수명·Evidence/State 딕셔너리
  원문은 **개발자 전용 개념**이며, 학생에게는 그대로 노출하지 않는다.
- 목적: 학생이 (1) 문제를 풀 때마다 즉시 피드백을 받고, (2) 여러 과목·스킬에 걸친 자신의 학습을
  한눈에 파악하고 관리할 수 있게 한다.

## 2. 이번 범위(Scope) 결정 사항

| 항목 | 결정 |
|---|---|
| 주 사용자 | 학생 (교사/연구자용 화면은 이번 범위 아님) |
| 스케일 | **화면 2개**: (A) 과목별 전체 학습 현황 대시보드, (B) 문제 풀이 직후 즉시 피드백. 여러 "학생"을 비교하는 교사용 대시보드는 이번 범위 제외 |
| 시간축 | Retrievability 등 시간에 따라 변하는 State도 **망각곡선 그래프 없이**, 배지/짧은 문구로만 표현. 시계열 그래프는 이번 범위 제외 |
| Evidence-State 관계 노출 | 기본은 **자연어 설명 문장 1~2줄**로 대체하고, 원한다면 "자세히 보기"로 **색상 강조된 Evidence/State 카드**까지 펼쳐볼 수 있는 **옵트인 확장** 구조 (연결선 다이어그램은 만들지 않음) |

## 3. 화면 구성 개요

```
[화면 A] 전체 학습 현황 (과목별)  ──(스킬 카드 클릭)──▶  [화면 A′] 스킬 상세(자세히 보기 확장)
        ▲                                                        │
        │ (문제를 풀고 돌아오면 자동 반영)                              │
        └──────────────── [화면 B] 즉시 피드백 (문제 풀이 직후) ◀───── (연습하기 CTA)
```

- 화면 A는 앱 진입 시 기본 홈. 화면 B는 문제를 풀 때마다 뜨는 결과 화면.
- 두 화면은 5장의 콘텐츠 매핑(라벨 사전)을 공유한다 — 같은 State/Evidence를 같은 문구로 부른다.

## 4. 화면 A: 전체 학습 현황 (과목별 대시보드)

### 4-1. 목적

학생이 여러 과목에 걸쳐 "지금 뭘 잘하고 있고, 뭘 더 챙겨야 하는지"를 스스로 관리하는 진입 화면.

### 4-2. 정보 구조: 과목(Domain) → 스킬(Skill)

러너 모델의 State 카테고리가 이미 `STUDENT × SKILL`(스킬 단위 7개 State)과
`STUDENT × DOMAIN`(과목 단위 State: calibration)으로 나뉘어 있다. 이 계층을 그대로
화면 구조에 사용한다.

```
과목(Domain) 탭  ──▶  그 과목에 속한 스킬(Skill) 카드 목록
   예: 수학                예: Chain Rule, 이차함수, 인수분해 ...
```

> ⚠️ index.html의 시나리오 데이터는 "Chain Rule" 스킬 하나만 다루며, 과목/여러 스킬을
> 묶는 데이터 구조는 아직 없다. 화면 A를 구현하려면 `과목 → 스킬 목록 → 스킬별 최신 State`를
> 담는 데이터가 새로 필요하다 (9번 검토 섹션 참고).

### 4-3. 화면 구조 (와이어프레임 텍스트)

```
┌─────────────────────────────────────────┐
│ 과목 탭:  [수학] [영어] [과학] ...          │
├─────────────────────────────────────────┤
│ 과목 요약 배너 (STUDENT × DOMAIN state)     │
│   예: "예상과 실제가 꽤 잘 맞고 있어요"        │
├─────────────────────────────────────────┤
│ 스킬 카드 그리드 (STUDENT × SKILL, 스킬별 독립 표시) │
│  [카드] Chain Rule     능숙해지고 있어요         │
│  [카드] 이차함수        복습 추천 배지            │
│  [카드] 인수분해        확인 필요 배지            │
├─────────────────────────────────────────┤
│ 정렬/필터: "약한 스킬 먼저 보기" 토글          │
└─────────────────────────────────────────┘
```

- 스킬 카드는 Khan Academy의 스킬 타일 그리드, ALEKS의 파이 차트 드릴다운 패턴을 참고.
- "약한 스킬 먼저 보기" 정렬은 학생이 스스로 학습을 관리하게 돕는 핵심 액션(요청하신
  "과목별로 학습 관리" 요구를 충족하는 지점).

### 4-4. 스킬 카드 요약 로직 (7개 SKILL State → 카드 1장)

**집계(여러 State/여러 세션을 하나의 점수로 합산)는 하지 않는다.** 모델이 갖고 있는 State
값을 그대로, 있는 그대로 보여준다. 스킬은 과목 탭 안에서 각각 독립적으로 표시되며, 스킬끼리
또는 과목끼리 합쳐서 하나의 종합 점수를 만들지 않는다.

카드에는 7개 State를 모두 나열하지 않고, 그중 학생이 체감할 수 있는 항목만 우선 노출한다
(값을 섞어서 새 지표를 만드는 게 아니라, 노출할 항목을 고르는 것만 우선순위로 결정).

| 표시 요소 | 반영 State | 규칙(제안) |
|---|---|---|
| 상태 표시(예: "능숙해지고 있어요") | `conceptual_mastery`, `procedural_mastery` | 두 값을 합산하지 않고 각각 짧은 문구/아이콘으로 그대로 표시 |
| "복습 추천" 배지 | `retrievability` 낮음 | 그래프·점수화 없이 배지 한 개로만 표시(시간축 제외 결정 유지) |
| "확인 필요" 배지 | `misconception` 있음 | 반복 오류 후보가 있을 때만 노출 |
| "도움이 필요해요" 배지 | `help_need` 높음 | 최근 힌트 의존도가 높을 때만 노출 |
| 자세히 보기 전용 | `transferability`, `state_confidence` | 카드에는 안 보이고, 스킬 카드 클릭 시 펼치는 상세에서만 노출 |

- 카드 자체는 상태 문구 1줄 + 배지 최대 2개까지만. 정보 과다 방지(교사용 히트맵과 달리
  학생용은 "단순화·우선순위화" 원칙을 더 엄격히 적용).

### 4-5. 과목 레벨 위젯 (DOMAIN / DOMAIN×INTERVENTION State)

- `calibration`(STUDENT × DOMAIN): 과목 요약 배너에 "예상과 실제가 얼마나 맞았는지" 한 문장으로.
- `intervention_response`(STUDENT × DOMAIN × INTERVENTION): 학생에게 직접 노출하지 않음
  (어떤 힌트 방식이 이 학생에게 잘 통하는지는 내부 튜닝용 지표 — 화면 B의 "자세히 보기"에서도
  제외 대상으로 유지).

### 4-6. 스킬 카드 클릭 시 동작

카드를 누르면 같은 화면에서 아래로 펼쳐지는 "스킬 상세" 패널을 연다(새 화면 전환 없음,
화면 B의 "자세히 보기"와 동일한 옵트인 확장 패턴 재사용):

- 최근 풀이 이력 요약(짧게), 7개 SKILL State 전체를 6장 콘텐츠 매핑 라벨로 표시.
- "이 스킬 연습하기" CTA 버튼 → 문제 풀이로 진입 → 풀고 나면 화면 B(즉시 피드백)로 이어짐.

### 4-7. 리서치 근거

- Khan Academy: 스킬 = 타일 그리드 → 카드 그리드 구조에 반영(단, 레벨을 합산 점수화하지는 않음).
- ALEKS: 파이 차트로 "아는 것/배운 것/남은 것" 요약 후 드릴다운 → 클릭 시 상세를 펼치는 패턴만 반영
  (파이처럼 값을 합쳐 하나의 비율로 만들지는 않음).
- 교사용 대시보드 원칙(단순화·우선순위화·맥락화)을 학생용에도 적용하되 더 보수적으로
  (카드당 배지 최대 2개).

## 5. 화면 B: 문제 풀이 직후 즉시 피드백

한 화면에 위에서 아래로 4개 블록. 기본 노출은 1~3번, 4번은 접힌 상태(옵트인).

```
┌─────────────────────────────────────────┐
│ ① 결과 배너                                │
│   정답/오답 아이콘 + 한 줄 반응 문구           │
├─────────────────────────────────────────┤
│ ② 이번에 달라진 점 (최대 2~3장 카드)          │
│   [카드] 아이콘 + 친화적 State 문구 + 방향    │
├─────────────────────────────────────────┤
│ ③ 다음 추천 행동 (Teaching Decision)        │
│   강조 카드 1개, 명령이 아닌 제안 톤           │
├─────────────────────────────────────────┤
│ ▾ 자세히 보기 (기본 접힘, 옵트인)             │
│   - 감지된 Evidence 칩(색상 강조)            │
│   - 관련 State 카드 전체(색상 강조)           │
└─────────────────────────────────────────┘
```

- **① 결과 배너**: 정답/오답을 즉각적·감정적으로 전달 (Duolingo류 초록/빨강 배너 패턴).
- **② 상태 변화 카드**: `index.html`의 `stateUpdates` 중 변화가 있는 항목만, 최대 2~3장만 우선순위로
  노출한다(전부 나열하면 정보 과다). 우선순위 기준: `state_confidence`처럼 메타적인 항목보다
  `conceptual_mastery`, `procedural_mastery`, `misconception`처럼 학생이 체감할 수 있는 항목을 우선.
- **③ 다음 추천 행동**: `decisionTitle`/`decision`을 학생에게 말하듯 1인칭 유도형으로 재작성한다
  (교사 지시문이 아니라 학생에게 건네는 제안).
- **④ 자세히 보기**: 펼치면 지금 데모의 Evidence Dictionary/Student State 패턴(하이라이트 카드)을
  그대로 재사용하되, **원본 코드(`code` 태그의 snake_case id)는 학생 화면에 노출하지 않고** 사람이
  읽는 라벨/설명만 보여준다.
- 풀이를 마치고 이 화면을 벗어나면 화면 A의 해당 스킬 카드 게이지/배지가 갱신된 상태로 반영된다.

## 6. 콘텐츠 매핑: 내부 개념 → 학생 문구 (화면 A/B 공통)

### 6-1. Evidence → 학생 노출 라벨 (자세히 보기에서만 노출)

| 카테고리 | 내부 id | 학생 라벨 |
|---|---|---|
| PERFORMANCE | correct | 정답 |
| | incorrect | 오답 |
| | partial_success | 부분적으로 맞음 |
| ASSISTANCE | independent_success | 혼자 힘으로 해결 |
| | hint_requested | 힌트 요청 |
| | success_after_hint | 힌트 받고 해결 |
| | success_after_explanation | 설명 듣고 해결 |
| ERROR | first_error | 첫 실수 |
| | repeated_error | 반복되는 실수 |
| | misconception_candidate | 헷갈리는 개념 후보 |
| | self_correction | 스스로 수정함 |
| MEMORY/RETRIEVAL | immediate_retrieval_success | 바로 기억해 냄 |
| | delayed_retrieval_success | 시간이 지나도 기억함 |
| | delayed_retrieval_failure | 시간이 지나 잊어버림 |
| | relearning | 다시 배우는 중 |
| TRANSFER | novel_application_success | 새로운 문제에 응용 성공 |
| | novel_application_failure | 새로운 문제에 응용 실패 |
| METACOGNITION | confidence_report | 확신 정도 응답 |
| | perceived_understanding | "이해했다"는 응답 |
| | perceived_difficulty | 체감 난이도 응답 |
| | error_awareness | 실수를 스스로 알아챔 |
| PROCESS | response_time | 풀이 소요 시간 |
| | stuck_duration | 막힌 시간 |
| | attempt_count | 시도 횟수 |

### 6-2. State → 학생 노출 문구

| 내부 id | 카테고리 | 학생 문구(변화 있을 때) | 화면 A 카드 노출 | 화면 B 카드 노출 |
|---|---|---|---|---|
| conceptual_mastery | SKILL | "개념 이해가 [좋아지고/약해지고] 있어요" | 상태 문구로 표시(합산 없음) | 카드 |
| procedural_mastery | SKILL | "풀이 과정이 [능숙해지고/서툴러지고] 있어요" | 상태 문구로 표시(합산 없음) | 카드 |
| retrievability | SKILL | "이 내용을 [잘 기억하고/까먹기 시작했고] 있어요" | "복습 추천" 배지 | 카드 |
| transferability | SKILL | "다른 문제에도 잘 [적용하고/적용하기 어려워하고] 있어요" | 자세히 보기 전용 | 자세히 보기 전용 |
| help_need | SKILL | "필요한 도움이 [줄고/늘고] 있어요" | "도움이 필요해요" 배지 | 카드 |
| misconception | SKILL | "헷갈리는 부분이 [있는 것 같아요/줄고 있어요]" | "확인 필요" 배지 | 카드 |
| state_confidence | SKILL | *(학생 화면 기본 미노출 — 내부 신뢰도 지표)* | 자세히 보기 전용 | 자세히 보기 전용 |
| calibration | DOMAIN | "예상한 것과 실제 결과를 비교해볼게요" | 과목 요약 배너 | 자세히 보기 전용 |
| intervention_response | DOMAIN×INTERVENTION | *(내부 튜닝용, 학생 비노출)* | 미노출 | 미노출 |

### 6-3. Teaching Decision → 학생 제안 문구 톤 (화면 B ③)

- 지시형(교사용) 대신 **제안형·1인칭 유도형**으로 재작성.
  - 예) "개입을 줄이고 독립 수행을 확인" → "다음엔 힌트 없이 한번 도전해볼까요?"
  - 예) "간격 회상 연습을 우선" → "잠깐 다시 떠올려볼 시간이에요."
- Khanmigo 스타일 원칙 적용: **정답/결론을 대신 말해주지 않고, 다음 행동을 부드럽게 제안**한다.

## 7. 인터랙션 패턴

- **기본 노출 최소화 + 옵트인 확장(progressive disclosure)**: 화면 B는 로드 시 ①②③만 보이고,
  ④는 "자세히 보기" 토글로 펼친다. 화면 A의 스킬 카드도 동일하게 "클릭 시 아래로 펼치기" 방식.
- 옵트인 확장 시에만 `index.html`의 색상 강조(`is-active`) 패턴을 재사용 — 감지된 Evidence 칩과
  관련 State 카드에 파란색 강조. 다만 코드/`code` 태그는 제거하고 라벨+설명만 표시.
- 연결선 다이어그램(Evidence↔State를 선으로 잇는 관계도)은 만들지 않는다.
- 화면 A ↔ 화면 B 전환: 스킬 카드의 "연습하기" CTA → 화면 B, 화면 B에서 풀이를 마치면 화면 A로
  복귀(뒤로가기 또는 "현황으로 돌아가기" 버튼) 시 갱신된 게이지/배지를 보여준다.

## 8. 비주얼 가이드

- `index.html`의 디자인 토큰(색상 변수, radius, shadow)을 그대로 재사용해 톤을 통일한다.
  - `--blue`(강조/제안), `--green`(정답/긍정 변화), `--amber`(주의/헷갈림), `--red`(오답)를
    결과 배너·State 카드 방향(↑/↓)·화면 A 배지 색상에 공통 활용.
- 카드형 레이아웃(`.section-card`, `.state-item` 스타일)을 계승하되, 학생 화면에서는
  타이포를 더 크게(가독성), 카드/배지 수를 최소화한다.

## 9. index.html 대비 반영 정확도 검토

| 항목 | 검토 결과 |
|---|---|
| Evidence Dictionary (7개 카테고리, 24개 항목) | 6-1 표에 전부 1:1 반영 확인. 누락 없음 |
| Student State (3개 그룹, 9개 항목) | 6-2 표에 전부 1:1 반영 확인. 누락 없음 |
| 시나리오 9개 | 화면 B ③의 톤 재작성 예시가 이 9개 시나리오의 `decision` 텍스트를 소스로 함 |
| **과목(Domain)/여러 스킬 데이터** | ⚠️ **index.html에는 없음.** 데모는 "Chain Rule" 스킬 1개만 다룸. 화면 A는 `STUDENT × DOMAIN`, `STUDENT × DOMAIN × INTERVENTION` 카테고리명이 이미 전제하는 "과목 계층"을 실제 화면으로 구현하는 것이므로 모델 설계 의도와는 맞지만, **여러 과목·여러 스킬에 대한 시나리오/State 데이터 자체는 새로 만들어야 함** |
| Evidence → State 매핑(어떤 Evidence가 어떤 State를 바꾸는지) | index.html의 `scenario.stateUpdates` 로직을 그대로 계승(화면 B ②). 화면 A는 그 결과로 남은 State의 **현재 값을 집계 없이 그대로** 보여주는 화면이므로, 별도의 합산/가중치 로직은 필요하지 않음(결정 사항, 아래 참고) |

## 10. 집계(Aggregation) 관련 결정

- **여러 State 값을 하나의 점수로 합치는 로직은 만들지 않는다.** 스킬 카드·과목 요약 모두
  모델이 갖고 있는 State 값을 있는 그대로(문구/배지) 보여준다.
- **과목끼리도 합쳐서 하나의 전체 진행도로 만들지 않는다.** 과목 탭으로 완전히 분리해서
  보여주고, 학생이 과목을 옮겨 다니며 각각 확인하는 방식으로 충분하다.
- 따라서 4-4의 "메인 게이지" 아이디어는 폐기하고, State별 개별 문구/배지 노출로 대체했다
  (반영 완료, 위 4-4·6-2 참고).

## 11. 이번 범위에서 제외한 것 (Out of scope)

- Retrievability 등의 시계열/망각곡선 그래프 (화면 A에서도 배지로만 대체)
- Evidence-State 관계를 잇는 hover 연결선 다이어그램
- 교사/연구자용 다중 "학생" 비교 대시보드 (원래 `index.html`이 담당하는 영역과는 별개로 유지)
- `intervention_response` 등 내부 튜닝 전용 지표의 학생 노출
- 여러 State/여러 과목을 합산하는 집계 로직 (10번 참고 — 의도적으로 만들지 않음)

## 12. 다음 단계

1. 화면 A에 필요한 데이터 구조(과목 → 스킬 목록 → 스킬별 최신 State) 정의 — 9번 검토에서
   확인된 갭을 메우는 작업. (집계 로직은 불필요하므로 "최신 값을 그대로 담는" 구조면 충분)
2. 위 콘텐츠 매핑 표를 바탕으로 화면 A(과목 대시보드 1개)와 화면 B(정답/오답 각 1개 시나리오)의
   실제 와이어프레임(HTML) 초안 제작.
3. "자세히 보기" 확장 인터랙션을 `index.html`의 기존 JS 패턴(scenario 선택 로직) 재사용해 프로토타이핑.
